import { CodeProps, TxtProps } from "@motion-canvas/2d"

export namespace styles {
	export const TXT: TxtProps = {
		fontSize: 40,
		fill: "#eee",
	}

	export const CODE: TxtProps = {
		fontFamily: "Jetbrains Mono",
		fontSize: 45,
	}
}
